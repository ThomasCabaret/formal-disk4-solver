# Legacy reference modules

These files are the source modules supplied with the initial project archive. They are kept only for traceability and are not imported by the new `formal_disk4` package. Several dependencies of the legacy pipeline were not present in the supplied archive, so the new prototype ports only the reusable word-equation ideas and exposes clean hooks for the remaining filters.
